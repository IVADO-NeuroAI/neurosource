# Papers

This directory is reserved for optional paper notes or short summaries that complement the structured catalog.

Suggested layout:

```text
papers/
`-- <year>/
    `-- <paper_slug>.md
```

These notes are optional and should support the catalog rather than replace structured metadata.
